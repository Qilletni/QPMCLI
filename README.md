# Qilletni Package Manager

QPM is the CLI utility used for managing remote and local packages. It connects to the [qpm-backend](https://github.com/Qilletni/qpm-backend), which is a serverless backend to host packages hosted by Cloudflare Workers,

For usage, see the [Package Management](https://qilletni.dev/packages/package_management) docs.
